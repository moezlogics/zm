{
  "name": "admin-orders-app",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "description": "Admin Orders PWA — view orders, change status, get push on new orders.",
  "scripts": {
    "dev": "node gen-manifest.mjs && node gen-icons.cjs && npx vite",
    "build": "node gen-manifest.mjs && node gen-icons.cjs && npx vite build",
    "preview": "npx vite preview --host"
  },
  "dependencies": {
    "react": "^18.3.1",
    "react-dom": "^18.3.1",
    "react-router-dom": "^6.26.2",
    "recharts": "^3.8.1"
  },
  "devDependencies": {
    "@types/react": "^18.3.3",
    "@types/react-dom": "^18.3.0",
    "@vitejs/plugin-react": "^4.3.1",
    "typescript": "^5.6.2",
    "vite": "^5.4.8"
  }
}
